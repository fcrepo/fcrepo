<?xml version="1.0" encoding="UTF-8"?>
<configuration xmlns="urn:activemq"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="urn:activemq /schema/artemis-configuration.xsd">

    <core xmlns="urn:activemq:core">
        <name>localhost</name>
        <persistence-enabled>true</persistence-enabled>

        <bindings-directory>${activemq.data.dir:./data}/bindings</bindings-directory>
        <journal-directory>${activemq.data.dir:./data}/journal</journal-directory>
        <paging-directory>${activemq.data.dir:./data}/paging</paging-directory>
        <large-messages-directory>${activemq.data.dir:./data}/large-messages</large-messages-directory>

        <acceptors>
            <acceptor name="artemis">tcp://0.0.0.0:${fedora.jms.port:61616}?protocols=CORE,OPENWIRE,STOMP</acceptor>
        </acceptors>

        <address-settings>
            <address-setting match="#">
                <dead-letter-address>DLQ</dead-letter-address>
                <expiry-address>ExpiryQueue</expiry-address>
                <redelivery-delay>0</redelivery-delay>
                <max-size-bytes>1gb</max-size-bytes>
                <address-full-policy>PAGE</address-full-policy>
                <auto-create-queues>true</auto-create-queues>
                <auto-delete-queues>false</auto-delete-queues>
                <auto-create-addresses>true</auto-create-addresses>
                <auto-delete-addresses>false</auto-delete-addresses>
            </address-setting>
        </address-settings>

        <addresses>
            <address name="DLQ">
                <anycast>
                    <queue name="DLQ" />
                </anycast>
            </address>
            <address name="ExpiryQueue">
                <anycast>
                    <queue name="ExpiryQueue" />
                </anycast>
            </address>

            <!-- Queue mode: use this when Fedora should publish to /fedora as a JMS queue -->
            <address name="/fedora">
                <anycast>
                    <queue name="/fedora" />
                </anycast>
            </address>

            <!--
            Topic mode alternative:
            replace the anycast block above with this multicast block if Fedora should
            publish to /fedora as a JMS topic instead.

            <address name="/fedora">
                <multicast/>
            </address>
            -->
        </addresses>

        <security-settings>
            <security-setting match="#">
                <permission type="createNonDurableQueue" roles="amq"/>
                <permission type="deleteNonDurableQueue" roles="amq"/>
                <permission type="createDurableQueue" roles="amq"/>
                <permission type="deleteDurableQueue" roles="amq"/>
                <permission type="createAddress" roles="amq"/>
                <permission type="deleteAddress" roles="amq"/>
                <permission type="consume" roles="amq"/>
                <permission type="browse" roles="amq"/>
                <permission type="send" roles="amq"/>
                <permission type="manage" roles="amq"/>
            </security-setting>
        </security-settings>
    </core>
</configuration>